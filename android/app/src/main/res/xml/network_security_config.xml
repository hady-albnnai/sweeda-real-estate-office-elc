<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config — HTTPS only, no certificate pinning.
  Certificate pinning معطّل حالياً لتجنب HandshakeException
  عند تغيير شهادات Supabase.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- debug overrides — للاختبار المحلي فقط -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
