# 📋 ملخص جولتَي 2026-07-28 / 29 — البناء والتصوير والفجوات الثلاث

> **ملاحظة توثيقية:** ثلاثة تقارير كُتبت أثناء الجولة ثم ضاعت عند إعادة استنساخ
> الريبو (بيئة الوكيل تُجرّد `.git` بين الجولات). هذا الملف يجمع خلاصتها الموثّقة
> بالأدلة، ويحل محلها: `BUILD_FIX_CDN_MIRROR` · `E2E_REMOTE_ROUND` · `PHOTOGRAPHY_E2E`.
> التقرير الرابع `2026_07_28_PHOTOGRAPHY_NOTIFICATIONS.md` سليم وموجود.

---

## 1) 🔧 إصلاح البناء — لم يكن بالكود ولا بإصدار Flutter

| الفحص | النتيجة |
|---|---|
| `flutter pub get` على 3.44.8 | ✅ 220 تبعية |
| `flutter analyze` على 3.44.8 | ✅ **0 أخطاء** (16 ملاحظة فقط) |
| Gradle 8.14.2 / AGP 8.11.1 / KGP 2.2.20 | ✅ فوق عتبة الخطأ |

**الجذر:** فشل DNS لـ `storage.googleapis.com` عند المالك ⇒ `No such host is known`.
**الحل:** مرآة CDN بمتغيّري بيئة (مرة واحدة للأبد):
```powershell
setx FLUTTER_STORAGE_BASE_URL "https://storage.flutter-io.cn"
setx PUB_HOSTED_URL "https://pub.flutter-io.cn"
```
ثم `flutter precache --android` + `flutter clean` + `pub get`. **نجح عند المالك ✅**

📌 قِطع المحرّك تُخزَّن تحت **hash المحرّك** (`0cd610717b`) لا hash الإطار — أي تنزيل يدوي بمسار الإطار يرجع 404 دائماً.
📌 **لا `flutter upgrade/downgrade`** — دستور §15 سليم ومُثبت: المشكلة كانت شبكة لا إصدار.

---

## 2) 🔒 السحب الأمني الشامل — 176 دالة

| الصنف | العدد | القراءة |
|---|---|---|
| `42501` محصّنة | **157** | ✅ الهدف |
| `PGRST202` (RETURNS trigger) | 18 | ✅ سليم بالتصميم |
| `PGRST203` (overload) | 1 | ✅ فُضَّ يدوياً ⇒ 42501 |
| **تسريب** (`P0001`/`22P02`/scalar) | **0** | ✅ |

- كل الإيدج (33 منشورة) ترفض بلا جلسة: `AUTH_TOKEN_REQUIRED` / `ADMIN_SESSION_REQUIRED` / `STAFF_SESSION_REQUIRED`.
- RLS: anon يقرأ `offers` + `app_config` فقط؛ INSERT/UPDATE/DELETE ⇒ `42501` على 11 جدولاً.
- `app_config` بلا أسرار (مسح آلي: صفر).

**⚠️ درس منهجي:** بروب INSERT بأعمدة وهمية يعطي `PGRST204` (عمود غير موجود) — **وهذا ليس رفضاً أمنياً**. أُعيد بأعمدة السكيما الحقيقية فظهر `42501` الصادق.
**تصحيح لسجل 2026-07-27:** كان مكتوباً أن `ratings` يقبل الإدراج والتريغر يحرسه — الواقع: **يرفض بـ42501 على مستوى الصلاحية**، أقوى مما وُثّق.

### انحراف مسجَّل
- `send-whatsapp-otp` + `verify-whatsapp-otp` **غير منشورتين** — بلا أثر (العميل يستخدم SMS فقط).
- `internal_notifications` و`notify_user_channels` **غير موجودتين حيّاً** رغم ذكرهما بالـHANDOFF كأصول حيّة.
- `payment_proofs`: anon يستطيع الرفع (مقصود جزئياً)؛ القراءة/الحذف مرفوضان ✅ — لكن بلا حدّ حجم/نوع.

---

## 3) 📸 مسار التصوير — اختبار E2E كامل (5 حلقات)

نُفِّذ فعلياً بأدوار: **المالك = مستخدم2عادي** · **الوكيل = المدير ثم المصوّر**.

`sts`: `0` بانتظار · `1` قيد التنفيذ · `2` مرسلة للمكتب · `3` معتمدة · `4` مرفوضة · `5` ملغاة

| الحلقة | النتيجة |
|---|---|
| الطلب | ✅ + إشعار **13 موظف** |
| الإسناد | ✅ موعد 2026/08/01 الساعة 11:00 |
| البدء | ✅ `0→1` |
| التسليم | ✅ `1→2` + 14 إشعار |
| الاعتماد | ✅ `2→3` |

**سبب الإلغاء** يُحفظ في `photography_tasks.office_note` بصيغة `"إلغاء من العميل: {السبب}"` — مُثبت: `"إلغاء من العميل: هيك"` ✅

### الفجوات الست المُغلقة (commit `33e5b02`)
1. الساعة كانت محفوظة لكن `_fmtDate` تحذفها عرضياً ⇒ `_fmtDateTime`
2. إشعار المصوّر مقتضب ⇒ غني (موعد + موقع + هاتف الطالب)
3. صاحب الطلب لا يُشعَر عند الإسناد ⇒ إشعار جديد (اسم + هاتف المصوّر)
4. **لا بوش خارجي**: مسار التصوير كان يكتب صف `notifications` بلا استدعاء `send_push_notification` ⇒ أُضيف داخل `notifyUsers`
5. غياب عن «مواعيدي» ⇒ دمج بطبقة الإيدج (بلا لمس بنية الجدول)
6. فجوتا البدء والتسليم ⇒ إشعارات جديدة

**التذكير قبل ساعة** (`send_photography_reminders`): رباعي تحصين ✅ · dedup بـ`rmnd_1h` ✅ · كرون `*/15` ✅
**البوش مُثبت:** `{"success":true,"sent":1}` × 4 لجهاز المالك.
**قناة المصوّر سليمة:** توكن وهمي ⇒ FCM رفضه ونظّفه (`cleanedUp:1`) ⇒ لو دخل بجهاز حقيقي يصله.

---

## 4) 💰 أجر التصوير (commit `1c0c1f0`)

- `ConfigModel.photographyPrice` ← `photoPrice` (افتراضي 1000، و**0 = مجاني فيُخفى**)
- بطاقة بشاشة الخدمة + تذكير داخل نموذج الطلب قبل التأكيد
- حقل بمحرر الإعدادات

---

## 5) ✅ الفجوات الثلاث المعلّقة — أُقفلت (commit `f7c95c1`)

### ① الإعلان المميّز المدفوع لا يظهر مميّزاً
`approve_payment_final` تكتب `fms_end` دون `i_fms`، بينما الشارة والترتيب والكنس كلها مشروطة بـ`i_fms=1` ⇒ **الزبون يدفع ولا يحصل على شيء**، و`fms_end` يبقى يتيماً للأبد.
**مُتحقق:** «يتيني» `i_fms 0→1` · `orphans=0` · الكنس صار شرطه `fms_end` وحده.

### ② وثيقة الملكية مكشوفة
`doc_img` رابط `public` يُجلب بلا مفتاح (200 · 289KB).
**⚠️ محاولة أولى فشلت:** `REVOKE SELECT` + منح عمودي ⇒ كسر التطبيق (`.select()` المفتوحة ترجع 42501 عند نقص أي عمود) ⇒ **تراجُع فوري** وإثبات رجوع 200.
**الحل:** جدول `offer_documents` محمي بـRLS + ترحيل + تريغر يحوّل أي كتابة لاحقة.
**مُتحقق:** `doc_img=''` · الجدول 401 للزائر · 3 وثائق محفوظة · `select(*)`=200.

### ③ زر «حفظ الإعدادات» معطّل خادمياً
`updateConfig` يكتب `app_config` بـPATCH مباشر بمفتاح anon ⇒ `42501` ⇒ **لا شيء يُحفظ رغم رسالة النجاح**.
**الحل:** أكشن `update_sections` بمفتاح الخدمة + قائمة بيضاء + دمج على مستوى القسم + سجل تدقيق.
**مُتحقق:** بلا جلسة⇒401 · `pkg`⇒`SECTION_NOT_ALLOWED` · `"abc"`⇒`INVALID_PHOTO_PRICE` · حفظ ناجح و**29 مفتاح بلا فقدان**.
📌 اكتُشف أن `socialPublishing` كان **مفقوداً من السيرفر** (سابق للتعديل) — أُضيف.

---

## 6) 🔜 المتبقّي (يحتاج قرار المالك)

| # | البند | الملاحظة |
|---|---|---|
| 1 | `payment_proofs` بلا حدّ حجم/نوع | فتحة إغراق تخزين — سياسة MIME + حجم أقصى |
| 2 | وثائق قديمة على باكيت public | الروابط المحفوظة مسبقاً تبقى تعمل؛ الإقفال الكامل = نقل لباكيت خاص + signed URL |
| 3 | `internal_notifications` / `notify_user_channels` | غير موجودتين حيّاً رغم التوثيق |
| 4 | واتساب OTP | الإيدجان غير منشورين (بلا أثر حالياً) |
| 5 | `supabase db dump` | يحتاج Docker — يُقفل دَين الانحراف ريبو↔سيرفر |
| 6 | أدوار لم تُختبر | الوسيط · المحامي/المعقب · الدفعات والباقات E2E |

---

## 7) 🧹 النظافة
- صفر جلسات وكيل نشطة (`revoke_all_staff_sessions` بعد كل جولة) — **جلسة المالك على جهازه لم تُمس**.
- المهام والبيانات الاختبارية أُعيدت لحالتها.
- **التوكنات** (GitHub PAT + Supabase PAT) لم تُكتب في أي ملف بالريبو — عاشت في `/tmp` ومتغيرات شل فقط. **على المالك حذفها عند الإطلاق.**
