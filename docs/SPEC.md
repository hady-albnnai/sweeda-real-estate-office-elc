# مستند مواصفات تطبيق "عقارات السويداء"
> مكتب عقاري إلكتروني — محافظة السويداء (مبدئياً)
> تطبيق موبايل — Backend: Supabase (PostgreSQL + Auth + Realtime + Storage + RPC Functions)
> هذه النسخة **نهائية ومعتمدة للتسليم للمبرمج**

---

## 🧠 فلسفة التصميم (مهم جداً للمبرمج)

> **كل شيء يُقرأ من Config — التطبيق مجرد "عارض" للإعدادات.**

- التطبيق لا يحتوي على أي أرقام أو نصوص ثابتة في الكود (Hardcoded).
- عند بدء التشغيل، يُحمّل `config` مرة واحدة ويُخزّن محلياً (Cache).
- أي تعديل في Config = تعديل في سلوك التطبيق فوراً. بدون تحديث للمتجر.
- استخدم **أسماء حقول قصيرة** (حتى 3 أحرف) لتقليل حجم البيانات المنقولة.
- استخدم **PostgreSQL RPC Functions لمعظم المنطق** — قلّل حجم التطبيق وزيّن الأمان.

---

## 🔖 اصطلاحات التسمية (Naming Conventions)

| الأصل | الكود | مثال |
|-------|-------|------|
| المعرفات | حقول قصيرة (2-4 أحرف) | `uid`, `sid`, `sts`, `typ` |
| التايمستامب | `ts` + اسم الحدث | `tsCrt`, `tsPub`, `tsEnd` |
| الحالات (Status) | أرقام (وليس نصوص) | `0`=مسودة, `1`=نشط, `2`=مكتمل |
| المراجع (Refs) | اسم المجموعة + `Id` | `usrId`, `offId`, `appId` |
| البوليان | حرف `i` قبل الاسم | `iPub` = منشور؟, `iDel` = محذوف؟ |

---

## 💰 استراتيجية توفير التكلفة (Cost-Saving Strategy)

### ⚡ مبادئ أساسية:
1. **التطبيق لا يعمل دون Config** — يُحمّل config عند كل start ويُخزّن محلياً.
2. **تجنّب Subcollections** — استخدم Maps داخل المستند نفسه لتجنب قراءات إضافية.
3. **Counters داخل المستند** — لا تقم بعمل `.count()` على Collection أبداً.
4. **الحذف الناعم (Soft Delete)** — استخدم `iDel` بدلاً من حذف المستندات.
5. **TTL Policies** — الإشعارات وأنشطة المستخدم تُحذف تلقائياً بعد 30 يوم.

### ⚡ PostgreSQL RPC Functions (المحرك الفعلي للتطبيق):
> **لا تعتمد على العميل لتنفيذ المنطق الحساس.** كل العمليات المهمة تذهب عبر RPC Function للتأكد من صحتها ولتسجيلها.

| الـ Function | Trigger / Usage | ماذا تفعل |
|-------------|---------|----------|
| `onNewOffer` | offer.create | تفحص تكرار العرض، تبدأ المطابقة التلقائية، تُرسل إشعار للإدارة |
| `onOfferApproved` | offer.update → iPub=true | تُنشئ إشعاراً للناشر، تبدأ المطابقة، تُنشئ منشور السوشيال ميديا |
| `onAppointmentCreated` | appointment.create | تُرسل إشعارات لجميع الأطراف، تتحقق من الحدود |
| `onAppointmentComplete` | appointment.sts=2 | تُنشئ بطاقة الموعد، تُرسل إشعارات |
| `onDealInitiated` | offer.sts=5 (محجوز) | تُلغي باقي المواعيد، تُرسل إشعارات، تحتسب العمولة |
| `onDealCompleted` | deal.create | تُفعّل النقاط والمكافآت، تُحدّث إحصائيات المستخدم |
| `onUserReport` | report.create | تُرسل تنبيه للإدارة |
| `hourlyTick` | Scheduled (كل ساعة) | تُرسل تذكيرات المواعيد القريبة (قبل ساعتين) |
| `dailyTick` | Scheduled (كل يوم) | تُنهي العروض المنتهية، تُرسل إشعارات التجديد، تنظف الإشعارات القديمة |
| `compressImage` | offer.create (via Edge Function) | تضغط الصور تلقائياً لتوفير المساحة |

---

## 🗂️ هيكل PostgreSQL النهائي (مُحسّن للتكلفة)

```
supabase/
│
├── ⚙️ app_config/             ← [مستند واحد] كل إعدادات التطبيق
│   └── main/                  ← جميع الإعدادات في مستند واحد
│
├── 👤 users/{uid}
│   ├── nm ← الاسم
│   ├── ph ← الهاتف
│   ├── ad ← العنوان
│   ├── role ← المستوى: 0=مستخدم, 1=وسيط, 2=مشرف, 3=نائب, 4=مدير
│   ├── sid ← رقم الهوية
│   ├── img ← URL صورة البطاقة
│   ├── pt ← رصيد النقاط
│   ├── bg ← البادج: 0=جديد, 1=برونزي, 2=فضي, 3=ذهبي, 4=ماسي
│   ├── bg_ts ← تاريخ آخر ترقية
│   ├── b_pkg ← الباقة: 0=مجاني, 1=فضي, 2=ذهبي
│   ├── pkg_end ← تاريخ انتهاء الباقة
│   ├── brk ← هل وسيط؟ 0=لا, 1=نعم
│   ├── brk_cls ← فئة الوساطة
│   ├── brk_nm ← الاسم التجاري
│   ├── sts ← حالة الحساب: 0=نشط, 1=مجمّد, 2=محظور
│   ├── ban_rsn ← سبب الحظر
│   ├── ntf ← إعدادات الإشعارات (JSONB)
│   ├── stats ← إحصائيات (JSONB)
│   ├── wk_lgn ← آخر تسجيل دخول أسبوعي
│   ├── strk ← streak counter
│   ├── strk_dt ← آخر يوم streak
│   ├── i_del ← 0=موجود, 1=محذوف
│   ├── ts_crt ← تاريخ الإنشاء
│   └── ts_upd ← تاريخ آخر تحديث
│
├── 🏠 offers/{offId}
│   ├── typ ← نوع العرض: 0=عقار, 1=سيارة
│   ├── trx ← نوع المعاملة: 0=بيع, 1=إيجار
│   ├── cat ← النوع الرئيسي
│   ├── sub ← النوع الفرعي
│   ├── ttl ← العنوان
│   ├── prc ← السعر
│   ├── cur ← العملة: 0=$, 1=ل.س
│   ├── loc ← الموقع (JSONB): {r:0, d:""}
│   ├── descript ← الوصف
│   ├── imgs ← روابط الصور (JSONB array)
│   ├── vdo ← رابط الفيديو
│   ├── doc_tp ← نوع سند الملكية
│   ├── doc_img ← رابط صورة السند
│   ├── exact_loc ← الموقع الدقيق
│   ├── specs ← المواصفات (JSONB)
│   ├── com ← عمولة المكتب
│   ├── sts ← الحالة: 0=مسودة, 1=قيد المراجعة, 2=منشور, 3=مرفوض, 4=منتهي, 5=محجوز, 6=مكتمل
│   ├── rsn ← سبب الرفض
│   ├── vws ← عدد المشاهدات
│   ├── fvs ← عدد المفضلة
│   ├── i_pub ← 0=غير منشور, 1=منشور
│   ├── i_soc ← نشر على السوشيال ميديا
│   ├── soc_pub ← حالة النشر
│   ├── soc_txt ← نص المنشور الجاهز
│   ├── i_dup ← مكرر محتمل
│   ├── dup_of ← offer ID إذا مكرر
│   ├── avl ← المواعيد المتاحة (JSONB)
│   ├── i_del ← حذف ناعم
│   ├── ts_crt, ts_pub, ts_end, ts_ren ← التواريخ
│
├── 📋 requests/{reqId}
├── 📅 appointments/{appId}
├── 🔔 notifications/{ntfId}
├── 💰 payments/{payId}
├── 📢 reports/{rptId}
├── 📊 deals/{dlId}
├── 📊 activity_log/{logId}
├── 📈 stats/{statId}
└── ⚙️ app_config/{key}
```

---

## ⚙️ مستند Config (المستند الوحيد اللي يتحكم بكل شيء)

### 🔥 كل ما يلي موجود في مستند واحد: `app_config/main`

```javascript
{
  // —— نقاط الاكتساب ——
  "pts": {
    "sgn": 1000, "wkL": 500, "addO": 500, "att": 300,
    "dlD": 2000, "ref": 1500, "strk": 200, "soc": 100,
    "like": {"p":5,"l":10}, "shr": {"p":10,"l":5},
    "cmt": {"p":20,"l":3}, "gft": {"max":500,"pw":1}
  },
  // —— خصم/صرف النقاط ——
  "pen": {"noSh":-500,"cnl3":-300,"rej3":-1000,"fRp":-2000,"ban":-40000},
  "spd": {"ren":500,"pin":2000,"bst":4000,"dsc5":3000,"fms":8000},
  // —— البادجات ——
  "bdg": {
    "0": {"nm":"🔰 جديد","p":0,"d":0},
    "1": {"nm":"🥉 برونزي","p":10000,"d":10},
    "2": {"nm":"🥈 فضي","p":20000,"d":15},
    "3": {"nm":"🥇 ذهبي","p":30000,"d":20,"eS":1},
    "4": {"nm":"💎 ماسي","p":40000,"d":20,"eS":1,"fA":1}
  },
  // —— الباقات ——
  "pkg": {
    "0": {"nm":"مجاني","o":5,"d":30},
    "1": {"nm":"فضي","o":15,"d":45},
    "2": {"nm":"ذهبي","o":40,"d":60}
  },
  // —— العمولة ——
  "com": {"sl": 3, "rn": "hm", "ml": 2},
  // —— الحدود ——
  "qta": {"u":{"o":1,"r":3,"a":3},"b":{"o":5,"r":5,"a":3}},
  // —— السوشيال ميديا ——
  "soc": {"fb":"","ig":"","tk":"","wa":""},
  // —— الإعلانات ——
  "ads": {"mx":5,"dd":7,"pr":null},
  // —— أسباب التبليغ ——
  "rptRsn": ["إعلان وهمي","احتيال","معلومات مضللة","مضايقة","عرض مكرر","آخر"],
  // —— نصوص التطبيق ——
  "txts": {
    "plg": "إقرار وتعهد إلكتروني...",
    "warnApp": "تحذير: هذا العرض عليه مواعيد سابقة",
    "visBlk": "تسجيل دخول مطلوب",
    "bnRsn": "تم حظر حسابك نهائياً",
    "frzRsn": "تم تجميد حسابك"
  },
  // —— أنواع العقارات ——
  "catProp": {...},
  // —— أنواع السيارات ——
  "catVeh": {...},
  // —— أنواع سند الملكية ——
  "docTp": {...},
  // —— قائمة المناطق ——
  "locs": [...],
  // —— قائمة الماركات ——
  "brnds": [...],
  // —— قائمة الألوان ——
  "clrs": [...],
  // —— صلاحيات الأدوار ——
  "roles": {"0":{"nm":"مستخدم"},"1":{"nm":"وسيط"},"2":{"nm":"مشرف"},"3":{"nm":"نائب"},"4":{"nm":"مدير"}}
}
```

---

## 🔒 قواعد الأمان (RLS Policies)

- **app_config**: الجميع يقرأ، الإدارة فقط تكتب
- **users**: صاحب الحساب والإدارة فقط
- **offers**: الجميع يقرأ المنشور، الناشر والإدارة يكتبون
- **appointments**: الأطراف المعنية والإدارة
- **notifications**: صاحب الإشعار والإدارة
- **reports**: المبلّغ يكتب، الإدارة تقرأ وتعدّل
- **باقي الجداول**: الإدارة فقط

---

## 📊 الـ Indexes الضرورية فقط

| Collection | الحقول | الغرض |
|---|---|---|
| offers | `typ`, `sts`, `prc`, `ts_crt` | عرض العقارات حسب السعر والتاريخ |
| offers | `sts`, `trx`, `ts_crt` | عرض البيع/الإيجار |
| offers | `usr_id`, `sts` | عروض مستخدم معين |
| appointments | `own_id`, `sts`, `dt` | مواعيد صاحب العرض |
| appointments | `req_id`, `sts`, `dt` | مواعيد طالب الحجز |
| notifications | `uid`, `i_rd`, `ts_crt` | إشعارات المستخدم |
| deals | `sell_uid`, `sts` | صفقات البائع |
| deals | `buy_uid`, `sts` | صفقات المشتري |

---

## 📌 خلاصة التوفير

| الاستراتيجية | التوفير |
|---|---|
| أسماء حقول قصيرة (3-4 أحرف) | 40-60% من حجم البيانات |
| Config يُحمّل مرّة ويُخزّن محلياً | آلاف القراءات في الشهر |
| Counters بدلاً من count() | 99% من عمليات العد |
| TTL على الإشعارات | تخزين أقل |
| RPC Functions بدلاً من client logic | كتابات أقل + منطق آمن |
| لا Subcollections | قراءات أقل |

---

## ✅ كل النقاط المعتمدة (1-12)

- [x] النقطة 1: الزائر — ✅ معتمدة
- [x] النقطة 2: المستخدم والتفعيل (OTP) — ✅ معتمدة
  - **تحديث 2026-06-05:** تم استبدال SMS بـ **WhatsApp OTP** (Meta Cloud API) + **Email Magic Link** كبديل، لأن SMS الدولي محجوب في سوريا. تفاصيل: `docs/AUTH_SETUP.md`
- [x] النقطة 3: الهيكل العام للواجهة — ✅ معتمدة
- [x] النقطة 4: تبويب العروض — ✅ معتمدة
- [x] النقطة 5: تبويب الطلبات — ✅ معتمدة
- [x] النقطة 6: رفع العرض والمراجعة — ✅ معتمدة
- [x] النقطة 7: حجز المواعيد — ✅ معتمدة
- [x] النقطة 8: موافقة الوسيط وصاحب العرض — ✅ معتمدة
- [x] النقطة 9: الوسيط ولوحته — ✅ معتمدة
- [x] النقطة 10: لوحة الإدارة والصلاحيات — ✅ معتمدة
- [x] النقطة 11: الإشعارات (داخلية + Realtime) — ✅ معتمدة
- [x] النقطة 12: نموذج البيانات (PostgreSQL + Config) — ✅ معتمد ومكتمل

---

## 📁 ملف locations.json

✅ موجود في `docs/locations.json` — **معتمد وجاهز.**

---

## 🗄️ بنية Supabase (مرجع سريع)

### الجداول (13)
`users` · `offers` · `requests` · `appointments` · `notifications` · `payments` · `reports` · `deals` · `activity_log` · `stats` · `app_config` · `otp_codes` · `user_devices`

### الدوال (12)
`generate_otp` · `verify_otp` · `generate_otp_v2` · `verify_otp_v2` · `upsert_user_after_otp` · `get_user_by_email` · `create_user_from_phone` · `get_user_by_phone` · `check_offer_duplicate` · `calculate_commission` · `update_user_badge` · `get_pending_offers_count` · `add_points` · `soft_delete` · `expire_offers` · `send_appointment_reminders`

**Edge Functions:** `send-whatsapp-otp`, `verify-whatsapp-otp`

### Realtime Tables
`offers` · `notifications` · `appointments` · `deals` · `requests`

### Storage
`offer_images` (Public bucket) — يُستخدم أيضاً لإثباتات الدفع تحت مسار `payments/{userId}/` (راجع `payment_screen.dart`). يمكن إنشاء bucket `payment_proofs` منفصل لاحقاً للأمان.
