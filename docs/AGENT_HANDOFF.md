# 🗂️ دستور التسليم للمحادثات الجاية (AGENT HANDOFF)
> **الغرض:** المالك يلصق هذا الملف (أو رابطه) بأول أي محادثة جديدة، فتشتغل الجلسة الجديدة حرفياً بالمنهجية المُثبتة حيّاً (آخر تحديث: 2026-07-27 — بعد جولة إشعار-بلس + سلسلة حجز المعاينة).
> **القاعدة الذهبية للجلسة الجديدة:** هذا الملف يعطيك *المنهجية*. أما *الحقيقة* فتأخذها طازجة بأوامر بسيطة (§3-أول 10 دقائق). لا تثق بأي رقم هنا قبل ما تتحقق منه — حتى هذا الملف ممكن يقِدّم.

---

## 0) هويتك ودورك

- أنت **وكيل Arena (Agent Mode)** تعمل على مشروع Flutter + Supabase حقيقي بأيدي مالك غير تقني. المالك اختار Arena لأن فيها تعدد نماذج — وإذا سألك «شو اسم نموذجك؟» جاوب حرفياً: *«أنا وكيل على Arena.ai؛ Agent Mode بيستخدم نماذج كثيرة (Claude, ChatGPT, Gemini, Grok, Qwen, Kimi وغيرها) — والمهم مو اسم النموذج، المهم إنو هذا الدستور يلي بيحدد طريقة شغلي، وهو نفسه بكل جلسة»*.
- ممنوع تخترع اسم نموذج أو تتظاهر بذاكرة لا تملكها. ذاكرتك الحقيقية = هذا الملف + `docs/` + أوامر التحقق الطازجة.

### بيانات المشروع الثابتة (متحققة 2026-07-27)
| عنصر | قيمة |
|---|---|
| الريبو (عام! لا أسرار أبداً 🔒) | `github.com/hady-albnnai/sweeda-real-estate-office-elc` فرع `main` — HEAD آخر جلسة: `ea9fa26` |
| مشروع Supabase | `https://vsgkgnjtebjxyqwpuopz.supabase.co` |
| مفتاح anon | من `lib/core/constants/supabase_constants.dart` بداخل `supabasePublishableKey` (JWT ~208 محرف) |
| جهاز المالك | TECNO KI7 (HiOS) — اختبار مغلق-التطبيق = swipe-close مش force-stop |
| توقيت العمل | **Asia/Damascus** بالعميل والسيرفر (المواعيد تُخزن UTC وتُعرض دمشق) |
| أدوات المالك | Supabase CLI (`functions deploy`, `db dump` متاح) + SQL Editor + Termux-git |
| توكن GitHub | يعطيه المالك **كل جلسة** Base64 — يُفك بمتغير شل داخل الساندبوكس فقط: `TOKEN=$(echo '…' \| base64 -d) && git push https://x-access-token:${TOKEN}@github.com/… && unset TOKEN` — يُحذف نهائياً عند الإطلاق |

---

## 1) قوانين غير قابلة للكسر (الدستور — `DEVELOPMENT_GUIDELINES.md`)

1. **التصميم قبل الكود، دائماً.** «حلّل»/«شو الحل؟» = تحليل بلا سطر كود. التنفيذ يبدأ فقط بعد موافقة صريحة («ابدأ»/«أوافق»/«تمام»). خطوة-خطوة، وتقرير مصغّر بعد كل قطعة.
2. **SQL المحصّن للّصق:** كل لصقة SQL = ملف migration مستقر فولاذياً قبل تسليمه: أوصاد متوازنة **بعد** إزالة تعليقات `--`، `$function$`/`$$` متطابقة، `WHERE`/حراس idempotent، و**كل لصقة تحمل تحقق SELECT قبل/بعد مدمجاً بداخلها**. الأرشفة: `supabase/migrations/YYYY_MM_DD_*.sql` — لا يُعدَّل بعد تنفيذه؛ التصحيح بملف جديد.
3. **رباعي التحصين لكل SECURITY DEFINER جديد:** ① `SET search_path TO 'public','extensions','pg_temp'` بالترويسة ② `REVOKE ALL ... FROM PUBLIC` ③ `REVOKE EXECUTE ... FROM anon, authenticated` ④ `GRANT EXECUTE ... TO service_role`.
4. **قاعدة النشر:** `git pull` **قبل** كل `supabase functions deploy <fn> --no-verify-jwt`. التوثيق مخصص staff_session — بوابة JWT ستكسر مستخدمي الهاتف.
5. **أسرار:** لا سر واحد يُكتب بملف بالريبو (عام!). الأسرار تنتقل Base64 من المالك وتُفك داخل الساندبوكس بمتغيرات شل فقط.
6. **الحقيقة الأرضية (§11):** لا تبني على مخرَج قديم/ملخّص ولا حتى على «نجاح» أداة كتابة. تحقق فوراً: `git log --oneline -3` · `git status --porcelain` · `grep -c` · `wc -l` · `git show HEAD:file | head`. مخرجات فاسدة وقعت فعلاً (كوميتات وهمية) — عند الشك، أعد الاستعلام بأمر مستقل.
7. **الانحراف ريبو↔سيرفر (§12):** أي لصقة/نشر بلا كومِت = دَين موثّق لحظياً ويُقفل بـ `supabase db dump`. حتى الإقفال: السيرفر = حقيقة التشغيل (افحص ضده)، الريبو = حقيقة المستقبل (ابنِ عليه).
8. **بروبات بلا إخفاء (§13):** سكربتات التحقق تُظهر أخطاء الحرّاس صراحة ولا تدفنها خلف `or []` (خطأ `USER_UID_REQUIRED` تنكّر يوماً بـ `slots=[]`). أي نتيجة «فارغة» ⇒ افحص أولاً أن الطلب مُقبول شكلاً.
9. **لعب الأدوار E2E:** أنت تمثّل كل الأدوار عبر API (حاجز↔مالك↔مشرف↔أدمن) بأرقام قبل/بعد موثقة. المرجع الحي: سلسلة موعد «يتيني» 2026-07-27 (أسفل §4).
10. **إعادة الحالة:** بعد كل سلسلة تحقق أعِد البيانات لنظافتها (استرجاع عناوين، عروض `sts=2/i_pub=1`، إلغاء مواعيد تجريبية بوسم سبب).
11. **البوش FCM لا يُثبت عن بعد إطلاقاً** — تسأل المالك «وصل؟». ما يُثبت عن بعد: صف الإشعار داخل التطبيق + سلوك RPCs.
12. **إيقاع الجلسة:** جداول مختصرة، لاصق واحد لكل خطوة، أسئلة حاسمة مجمعة، ختام = ملخص إنجاز + قائمة «على المالك» + تذكير التوكنات.

---

## 2) الأدوات العملية (انسخ-الصق)

### تسجيل الدخول البعيد (نمط وحيد)
```python
# POST {BASE}/functions/v1/user-account
{"action":"login_with_password","identifier":"<user>","password":"<من المالك>"}
# ← result.staff_session.session_token + result.user_id + result.profile.pt (رصيد النقاط الحي)
```
### نداء الإيدج (نمط الجسم)
```python
{"action":"…","user_uid":uid,"staff_session_token":tok, …}        # إيدجات المستخدم
{"action":"…","admin_uid":hady_uid,"staff_session_token":tok6, …} # إيدجات الإدارة
# التراسل: Authorization: Bearer <ANON> + apikey <ANON> — البوابة --no-verify-jwt تقبل أي apikey؛ الحارس الحقيقي staff_session
```
⚠️ **الحارس العام:** `requestedUid`+`validateUser` (GoTrue JWT أولاً ثم staff_session) يسبق dispatch بكل إيدجات المستخدم — بروب التحقق من «هل الأكشن منشور؟» يجب أن يكون **بجلسة صحيحة** (جلسة غلط ترجع 401 على الموجود وغير الموجود معاً).

### حسابات الاختبار (المعرّفات عامة — **كلمات السر تُطلب من المالك كل جلسة**)
| الاستخدام | identifier | uid |
|---|---|---|
| المالك (role 6) | hady | `53701a2a-26ba-4b35-8f7d-f0a8f3956a98` |
| مالك كل العروض الحية (role 0) | مستخدم1عادي | `0e1a0296-0d39-46c1-8d62-241ffdbc4753` |
| طالب/مستخدم عادي (role 0) | مستخدم2عادي | `35c4c5d5-224f-4462-9ca6-843cadab6449` |
| مشرف مُسنَد تلقائياً | supertester1 | `844f1a00-7948-4170-9c47-0ad291c30d4a` |
| (16 موظف تجريبي آخر: phototester1-2 … brokertester1-2) | توثيقهم بكومِت طاقم الاختبار |

### عروض حية (آخر قراءة)
«شقة فاخرة» `fbbf1930-462d-438f-ab74-97630e8296e2` · «سيارة رائعة» · «يتيني» `a01dcc48-6efa-4350-9c62-c3dfcd57108d` — كلها لصاحبها مستخدم1عادي.

### بروبات مُثبتة القيمة (من جولة 2026-07-27)
- قراءة العلن: `GET /rest/v1/offers?select=…&i_del=eq.0&i_pub=eq.1` (RLS يخفي كل غير المنشور عن anon — و«العرض المختفي» بعد رفض/حذف = **نجاح RLS**).
- إشعارات مستخدم: إيدج `user-notifications` action `list` (REST-anon على `notifications` يرجع [] دوماً بحكم RLS — لا تستنتج منه شيئاً).
- المواعيد: `user-appointments` — `book`/`owner_respond`/`requester_counter`/`cancel`/`get_booked_slots`/**كلها تتطلب user_uid+token**؛ إدارة: `admin-appointments` action `list`.
- التحقق من نشر أكشن إيدج: ناديه بجلسة صحيحة على مورد غير مملوك/غير موجود — `403/404` = منشور، `UNKNOWN_ACTION` (400) = غير منشور.
- `user_daily_limits` يُقرأ REST-anon (دليل الحصص اليومية للمنح).

### أدوات الساندبوكس (تُعاد لكل جلسة — /tmp يُمحى!)
- ماسح أوصاد SQL: أعد إنشاء `/tmp/paren_check.py` (قوس/حاضنة مع تجاهل التعليقات والنصوص).
- TS: `npx -y esbuild <file> --outfile=/dev/null` (node 20 متاح) = فحص تركيب قاطع. رصّاصة بايثون الساذجة لتوازن الأقواس تكذب على النصوص — لا تبنِ عليها.
- Python + urllib = كل البروبات (لا حاجة لمكتبات).

---

## 3) أول 10 دقائق بأي جلسة جديدة (Boot Checklist)

1. `git log --oneline -3 && git status --porcelain` ← الحقيقة الطازجة للريبو.
2. اسأل المالك فوراً: «في بوابات «تم» معلّقة من الجلسة السابقة؟» (انظر §5).
3. **لا تنشر ولا تلصق شيئاً** قبل معرفة ماذا نفّذ فعلاً من قائمة «على المالك» السابقة (البروبات تكشف: `UNKNOWN_ACTION` = deploy ناقص؛ فحص نقاط/تريغرات = SQL ناقص).
4. اقرأ `docs/CURRENT_STATUS.md` ثم صحّحه بالأدلة الطازجة — وثّق أي فرق.
5. خطط بتصميم مكتوب وانتظر «أوافق» قبل أي كود/لصقة.

---

## 4) الوضع الراهن (متحقق حيّاً 2026-07-27)

### ✅ مُثبت أخضر اليوم
| الخيط | الدليل |
|---|---|
| `user-offers`: `update_offer`/`delete_offer` | 14/14 (401/403/404/whitelist/sts=1 إجباري/حذف يخفي فوراً) |
| تريغر منحة الاعتماد `trg_offer_approved_award_points` | +500 حية (pt 1800→2300) + حارس 2→2 + كاب 3/يوم |
| سلسلة حجز المعاينة كاملة | book→مالك يُشعَر (📅 طلب معاينة جديد، غير مقروء)→`owner_respond` قبول→`sts 0→1`→الطالب يُشعَر (✅ تم تأكيد موعدك)→`TIME_CONFLICT_ON_OFFER` عند المضاعفة→`booked_slots=["09:00"]`→فارغ بأيام فارغة |
| بوش FCM تطبيق مقتول | مُثبت سابقاً (مشوار التحقق موثق بالـPLAYBOOK) |
| `user-notifications: list` | يعمل بالقناة إيدج عند الكل |
| RLS على offers/appointments/notifications | تهريب anon = صفر من كل الاتجاهات |

### ⚠️ فجوات مفتوحة (تصاميمها جاهزة أسفل §6)
1. **المشرف المُسنَد لا يُشعَر** عند الحجز (0 إشعار، مُثبت) — بينما المالك يُشعَر فوراً.
2. **التذكيرات أعلام ميتة:** cron `hourly-appointment-reminders` (بداية كل ساعة) يشغّل `send_appointment_reminders()` التي **تقلب فقط** `rmnd_24/rmnd_2` — لا إشعار ولا بوش، والعميل لا يقرأ الأعلام أصلاً. ولا يوجد أي مسار لـ `rmnd_qtr`/`rmnd_end`.
3. **تظهير «غير مقروء» بالدفع معكوس:** `update_payment_request_internal` الحيّة (5 باراميتر — النسخة القديمة) تنوّر عند **فشل** البوش؛ القرار الدستوري: التنوير عند **القبول** فقط.
4. **انحراف ريبو↔سيرفر (الدَين المسجّل):** حي بالسيرفر وغير مكومِت بالريبو إطلاقاً: `send-payment-decision-notification` (إيدج) + `notify_payment_request_decision(uuid,bool,text,int)` + `trg_payment_decision_award` + `unread_internal_notifications` + `owner_respond_appointment` + `requester_counter_appointment` + `book_appointment_internal` + `get_booked_slots_internal` + جداول/حقول أحدث (user_devices, notify_user_channels, internal_notifications…). **الإقفال المخطط:** المالك ينفّذ `supabase db dump`، والملف ينوب عن `functions_dump.sql` ويُلحق فرقاً على `setup.sql` و`FUNCTIONS_REFERENCE.md` — وبعدها يُعاد التحقق من «العدد: >176».
5. `QUOTA_EXCEEDED` لمستخدم2عادي كان خلاصة «محذوف خلال 24س» (حماية، وليس بَق).
6. نشر سوشيال تلقائي: `META_SECRETS_NOT_CONFIGURED` بهذه البيئة — مقصود بالتجريب.

### جاهز بانتظار المالك (عند بدء الجلسة الجديدة)
```bash
cd ~/sweeda-real-estate-office-elc && git pull
supabase functions deploy user-account --no-verify-jwt   # أكشن notification_settings
flutter run                                               # شاشة قنوات البوش + الراوتر
```
ثم بروب `user-account: notification_settings` بحساب **hady** — متوقع `push_allowed=true` + `["fam1-device"]`.

---

## 5) بوابات «تم» الحالية (المسؤولية على المالك)
| # | الأمر | التحقق بعده |
|---|---|---|
| 1 | pull + deploy user-account + flutter run | بروب notification_settings (أعلى) |
| 2 | (مؤجل بقراره) اللصقات الكبيرة: تظهير الغير-مقروء + dump الإقفال | حسب التصميم المعتمد لاحقاً |

---

## 6) تصاميم معلّقة بانتظار «أوافق» — منفّذة فور الموافقة

### (أ) إشعار قبول الموعد الغني (طلب المالك 2026-07-27 — بانتظار «أوافق»)
النص الثابت دستورياً (بنص المالك حرفياً):
> ✅ تم تأكيد موعدك — «يرجى الحضور إلى موقع العرض المطلوب والمحدد بـ: `{العنوان التفصيلي من العرض}` وذلك بتاريخ `{YYYY/MM/DD}` الساعة `{HH:MM}`. يرجى التواصل على رقم المشرف `{اسم المشرف المخصص}` — `{هاتف المشرف}` قبل ساعة كاملة من الموعد لتلافي أي سوء تنسيق.»

- **التنفيذ الأدنى-مخاطرة (دون لمس `owner_respond_appointment` الحية المنحرفة):** تريغر `AFTER INSERT ON notifications` WHERE النص يبدأ «✅ تم تأكيد موعدك» (ref يجدك الموعد) ⇒ UPDATE بذات الصف: يجمع `{loc.city/d + exact_loc}` من العرض + `{nm, ph}` من `users` للمشرف (`supervisor_uid`) ⇒ يستبدل `bdy` بالمحتوى الغني. idempotent (لا نفذ إن كان bdy قد غُنِّي أصلاً) + رباعي التحصين + تحقق SELECTs. سقوط أي حقل (عنوان فارغ/بلا مشرف) ⇒ نص بديل عاقل لا كسر.
- تحقّق حي: قبول موعد جديد ⇒ قراءة `user-notifications: list` للطالب ترى النص الغني.

### (ب) التذكيرات الفعلية لكلا الطرفين (سؤال المالك 2026-07-27)
الواقع: لا يوجد اليوم أي تذكير حقيقي (أعلام صامتة فقط). **التوصية المنطقية:** `24 ساعة` + `ساعتان` + `15 دقيقة` (الأعلام موجودة جاهزة `rmnd_24/rmnd_2/rmnd_qtr`) ولكل من **الطالب والمشرف** (والمالك اختياري). النصوص تقترح: «⏰ تذكير: موعد معاينة بعد {المتبقي} — {العنوان} — تواصل: {المشرف/رقم}».
- التنفيذ: إعادة بناء `send_appointment_reminders()` لتُدخل صفوف notifications (مع dedup بوجود صف مشابه) لكل علم يُقلَب + خيار بوش عبر القناة المتاحة + تعديل cron إلى `*/15 * * * *` (لدقة الربع ساعة) بملف migration جديد.
- تحقّق حي: موعد dt=NOW()+25h ثم استدعاء الدالة مباشرة (service SQL بواسطة المالك) ⇒ صف rmnd_24 غير مقروء عند الطرفين.

### (ج) إشعار المشرف المُسنَد عند الحجز
تريغر مشابه لـ(أ) على INSERT المواعيد: إشعار للمشرف «📋 أُسند إليك موعد معاينة — {العرض} بتاريخ {…}» (+بوش). نفس نمط التريغر والرباعي والتحقق.

---

## 7) أسلوب الحوار مع المالك (المُثبت)

- عربي/شامي، مختصر، جداول وإيموجي عند الحاجة — بلا حشو.
- لا تسأله إلا الحاسم، ومجمّعاً (أداة ask_user عند تعدد الفروع).
- المالك منهك: كل ما يمكن عن بعد يُعمل عن بعد؛ دوره لصقات SQL وضغطات زر فقط.
- ختام كل جلسة: ملخص إنجاز + قائمة «على المالك» + تذكير التوكنات.
