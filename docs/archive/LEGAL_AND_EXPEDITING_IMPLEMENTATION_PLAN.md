# 📋 خطة التنفيذ المنهجية التفصيلية لنظام القسم القانوني وتعقيب المعاملات (Legal & Expediting Implementation Plan)

> **المشروع:** تطبيق عقارات السويداء (`sweeda-real-estate-office-elc`)
> **التاريخ:** 2026-07-04
> **الحالة:** خطة تنفيذ تشغيلية معتمدة (Approved Execution Plan)
> **مبدأ الأمان الأول:** جميع التعديلات على قاعدة البيانات وسيرفر Supabase تلتزم بالتحصين الصارم (`SECURITY DEFINER` + `SET search_path` + منح `service_role` فقط للدوال الحساسة) وتنعكس فوراً في المستودع المرجعي (`functions_dump.sql` و `FUNCTIONS_REFERENCE.md`).

---

## 🧭 خارطة الطريق والمراحل التنفيذية

### 🟢 المرحلة 1: بنية قاعدة البيانات والدوال المحصّنة (Database Schema & Secure RPCs)
* **الدفعة 1.1:** ✅ مُنجز (تم إنشاء جدولي `lawyer_profiles` و `expediting_tasks` مع حقول الكتابة التفاعلية ورقم العقار/السيارة).
* **الدفعة 1.2:** ✅ مُنجز (تم كتابة وتطبيق دوال إدارة المحامين والتوزيع الآلي وتحديث بنود المعقبين بصلاحية `service_role`).
* **الدفعة 1.3:** ✅ مُنجز (تم عكس الجداول والدوال في مرجع الحقيقة `functions_dump.sql` وتوثيقها في `FUNCTIONS_REFERENCE.md`).

---

### 🟦 المرحلة 2: طبقة الاتصال والوساطة السحابية (Edge Functions & Network Layer)
* **الدفعة 2.1:** ✅ مُنجز (تم بناء دالة `legal-actions` السحابية).
* **الدفعة 2.2:** ✅ مُنجز (تم تزويدها بالتحقق المزدوج الصارم لضمان القبول الآمن للتوكنات دون رفض).

---

### 🟨 المرحلة 3: طبقة النماذج ومزودي الحالة في Flutter (Models & Providers)
* **الدفعة 3.1:** ✅ مُنجز (تم تحديث ثوابت الأدوار في `user_model.dart`: إضافة `lawyer = 7` و `expediter = 8`).
* **الدفعة 3.2:** ✅ مُنجز (تم إنشاء النماذج `LawyerProfileModel` و `ExpeditingTaskModel` و `ChecklistItemModel`).
* **الدفعة 3.3:** ✅ مُنجز (تم إنشاء مزود الحالة `LegalProvider` وربطه بتطبيق فلاتر الرئيسي).

---

### 🟧 المرحلة 4: شاشات الواجهات ومسارات العمل (UI Screens & User/Staff Flows)
* **الدفعة 4.1 (مسار العميل):** ✅ مُنجز (بناء شاشة حجز الاستشارات وباقة 700 ألف وزر واتساب المحامي المباشر).
* **الدفعة 4.2 (مسار المحامي):** ✅ مُنجز (بناء لوحة تحكم المحامي وجدولة أوقات المواعيد `avl`).
* **الدفعة 4.3 (مسار معقب المعاملات):** ✅ مُنجز (بناء شاشة المعقب التفاعلية وكتابة الأرقام الميدانية ورفع الصور).

---

## 📋 بروتوكول التنفيذ والمتابعة خلال المحادثة
1. نبدأ الآن فوراً بتنفيذ **المرحلة 1 (قاعدة البيانات والدوال الأمنية)**.
2. يتم تطبيق الأكواد على قاعدة البيانات الحية ورفع الالتزام للمستودع فور الانتهاء من كل دفعة لضمان التزامن 100%.
