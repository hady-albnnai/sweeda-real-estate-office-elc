# بدء اختبار المستخدم العادي (role=0) — 2026-07-12

**المرجع:** sweeda_test_plan_arabic.pdf — القسم 8 (المستخدم العادي)
**الغرض:** توليد بيانات حقيقية (عروض منشورة + طلبات نشطة + مواعيد) لتغذية باقي الأدوار.
**الالتزام بالدستور:** DEVELOPMENT_GUIDELINES.md (كود + توثيق + رفع)

## البيانات المُنشأة (مباشرة من السيرفر الحي)

### المستخدم العادي
- UID: `47678c02-a5a1-4786-884c-ba9eedc812a8`
- الهاتف: `+963930117881`
- الدور: 0
- الباقة: مجاني (0)

### العروض المنشورة (sts=2, i_pub=1) — 10 عروض
1. شقة 3 غرف في المزرعة — 85,000,000 USD
2. سيارة تويوتا كامري 2022 — 32,000,000 USD
3. أرض زراعية 5 دونمات — 120,000,000 USD
4. فيلا فاخرة مع حديقة — 250,000,000 USD
5. مرسيدس C200 2023 — 95,000,000 USD
(الباقي تكرارات من الجلسات السابقة)

### الطلبات النشطة (sts=0) — 8 طلبات
- شراء عقار: شقة 3 غرف قرب المدرسة (75 مليون)
- إيجار سيارة: سيارة اقتصادية (25 مليون)
- شراء فيلا مع حديقة (90 مليون)
- إيجار شقة طويل الأمد (40 مليون)

### المواعيد (sts=0)
- 3 مواعيد مستقبلية مرتبطة بعروض (15, 18, 21 يوليو 2026)

**الإعدادات المؤكدة من السيرفر:**
- `app_config.main.req` = {'d':30, 'ren':30, 'warn':3, 'purge':180}
- جميع أعمدة دورة الحياة موجودة
- الـ RPCs `create_request_internal` و `get_user_requests_internal` ... إلخ تعمل عبر Edge فقط

## خطوات بدء الاختبار الفعلي (من الخطة العربية)

### 8.1 المصادقة والملف الشخصي
1. سجل دخول بالهاتف `+963930117881` (أو OTP).
2. أكمل إعداد الملف (اسم مستخدم + كلمة مرور).
3. (اختياري) أكمل الهوية للتوثيق.

### 8.2 العروض
- تصفح الصفحة الرئيسية + فلاتر.
- شاهد تفاصيل العروض المولدة.
- أضف عرضاً جديداً (اختبر الحصة).
- جرب الترقية بالنقاط (إن وجدت).

### 8.3 الطلبات (التدفق الكامل — الأهم)
1. ادخل `/user/add-request`
2. اختر: شراء/إيجار + عقار/سيارة
3. أدخل بيانات العميل + الميزانية
4. أرسل الطلب (يجب أن يمر عبر `user-requests` Edge)
5. اذهب إلى "طلباتي" وتأكد من ظهور الطلب (sts=0)
6. من تفاصيل الطلب:
   - شاهد العروض المطابقة
   - احجز موعداً
7. اختبر:
   - إلغاء الطلب
   - تجديد الطلب (قبل الانتهاء)
   - التحقق من `ts_end` و `rmnd_ren`

### 8.4 المواعيد والإشعارات
- احجز موعداً على عرض منشور.
- رد على العروض المضادة (إن وجدت).
- شاهد "مواعيدي".
- تحقق من الإشعارات (داخل التطبيق + Realtime).

### 8.5 الباقات + الإحالة + التقييم + المفضلة
- اشترِ باقة.
- جرب كود الإحالة.
- أكمل موعد وأضف تقييم.

## التحقق المباشر (بعد كل خطوة)
استخدم السكريبت:
```bash
python3 /home/user/verify_requests_live.py
```

أو الاستعلامات:
```sql
SELECT sts, COUNT(*) FROM public.requests GROUP BY sts;
SELECT sts, COUNT(*) FROM public.offers WHERE i_del=0 GROUP BY sts;
```

## الخطوة التالية (بعد انتهاء دور المستخدم العادي)
- نقل البيانات إلى الأدوار الأخرى (وسيط، موظف، منفذ، محامي، معقب، مدير).
- تنفيذ اختبارات التكامل بين الأدوار.

**التاريخ:** 2026-07-12
**الحالة:** ✅ بيانات جاهزة + بدء الاختبار اليدوي الآن.