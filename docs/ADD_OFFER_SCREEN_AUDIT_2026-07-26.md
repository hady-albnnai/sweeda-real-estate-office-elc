# تدقيق شاشة إضافة العرض بكل الأدوار — 2026-07-26

## البلاغ (المستخدم، اختبار حقيقي على أندرويد)

> "بلشت بإضافة عرض واخترت سيارة — خيارات التصنيف الرئيسي طلعت تبع العقارات.
> وبعد تبويب الأساسيات ما عاد اشتغل شي."

## نتيجة التدقيق — 6 مشاكل مؤكدة

| # | المشكلة | السبب الجذري | الأدوار المتأثرة |
|---|---|---|---|
| 1 | تصنيفات "سيارة" تعرض تصنيفات العقارات | لا يوجد أي seed رسمي لـ `catVeh` في `app_config` (لا setup.sql ولا migrations) — القيمة الحية كتبت يدوياً سابقاً وتحوي محتوى عقارات | الكل (مستخدم/وسيط/موظف/مدير/نائب) |
| 2 | التوقف بعد تبويب الأساسيات | شاشة المستخدم Stepper **بدون أي أزرار تنقل** (`controlsBuilder` يخفي الافتراضية) — التنقل بالنقر على عنوان الخطوة فقط، وعناوين الخطوات غير النشطة رمادية فتُوهم أنها معطلة | مستخدم عادي + وسيط |
| 3 | تجميد كامل للشاشة (crash عند build) | `int.parse(e.key)` بدون حماية على مفاتيح `carDocTp`/`plateTp` + cast غير آمن `as List?` للتصنيف الفرعي — أي داتا مشوهة من السيرفر ترمي استثناء أثناء build | مستخدم عادي + وسيط |
| 4 | RangeError محتمل | التبديل عقار→سيارة وأنت بالخطوة الأخيرة (index 4) يقلّص الخطوات من 5 إلى 4 والـ `currentStep` يبقى 4 | مستخدم عادي + وسيط |
| 5 | المدير/النائب بلا مدخل لإضافة عرض | زر `/admin/add-offer` موجود فقط في شاشة الموظف — غير موجود بقائمة أقسام الإدارة `/admin/sections` (التي يستخدمها المدير والنائب) | مدير + نائب مدير |
| 6 | تضارب بوابة الصلاحية | الراوتر يفحص `review_offers` على `/admin/add-offer` بينما زر الموظف يفحص `add_offer_admin` | موظف مكتب |

## خريطة وصول الأدوار لإضافة عرض (بعد الإصلاح)

| الدور | الشاشة | المدخل | بوابة الراوتر |
|---|---|---|---|
| مستخدم عادي | `/user/add-offer` | تبويب "عروضي" (FAB) + "أضف عرضك الأول" بالرئيسية | `user_offers` |
| وسيط عقاري | `/user/add-offer` | نفس المستخدم | `user_offers` |
| موظف مكتب | `/admin/add-offer` | زر بشاشة الموظف + قائمة الأقسام | `add_offer_admin` |
| نائب مدير | `/admin/add-offer` | قائمة أقسام الإدارة | `add_offer_admin` |
| مدير | `/admin/add-offer` | قائمة أقسام الإدارة | `add_offer_admin` |

## الإصلاحات المطبقة بالكود

### `lib/screens/user/add_offer_screen.dart`
- **أزرار تنقل صريحة** (`_navRow`): "التالي"/"رجوع" في نهاية كل خطوة، والتالي بخطوة الأساسيات يتحقق أولاً (نوع العرض/المعاملة/التصنيف الرئيسي) ويعرض رسالة موجهة عند النقص — لم يعد التنقل يعتمد على النقر بالعناوين.
- **حماية من RangeError**: `currentStep` يُقصّ (clamp) لعدد الخطوات الفعلي كل build.
- **`context.watch` بدل `read`** لخطوة الأساسيات — التصنيفات كانت تصل من السيرفر بعد فتح الشاشة فتبقى القوائم فارغة حتى أول تفاعل.
- **قراءة آمنة للإعدادات**: `_intDropdownItems` (يتجاهل المفاتيح غير الرقمية/المكررة بدل int.parse)، `_subListOf` (يدعم sub كـ List أو Map)، `_mainCatName` (يدعم قيمة الماب أو النص).
- **صناديق تحذير + زر إعادة تحميل** (`_warnBox`) عند غياب التصنيفات/السندات بدل تجميد صامت.
- **`isActive` صحيح** لخطوة السند (`_lastStep` = 3 للسيارة، 4 للعقار) — كانت تظهر معطلة دائماً للسيارة.
- **حقل المحافظة للسيارة**: `_carGovernorate` كان يتيماً (لا ويدجت تعبئه) فتُحفظ السيارة بلا موقع — أضيف dropdown محافظة لخطوة الأساسيات.

### `lib/core/router/app_router.dart`
- بوابة `/admin/add-offer`: `reviewOffers` → `addOfferAdmin` (توحيد مع زر الموظف).

### `lib/screens/admin/admin_sections_screen.dart`
- بطاقة "إضافة عرض" → `/admin/add-offer` بصلاحية `add_offer_admin` — أصبحت متاحة للمدير/النائب (وللموظف أيضاً).

### `supabase/setup.sql` (بلوك نهائي بتاريخ 2026-07-26)
- seed رسمي idempotent لإعدادات إضافة العرض: `catProp`, `catVeh`, `docTp`, `carDocTp`, `plateTp`.
- الصيغة `defaults || value` تملأ **فقط المفاتيح الناقصة** — لا تمس أي قيمة موجودة حية.
- شاشة الأدمن (`admin_add_offer_screen.dart`) رُوجعت وأزرار التنقل فيها موجودة أصلاً بكل خطوة — لم تحتج تعديلاً.

## ⚠️ خطوة سيرفر يدوية مطلوبة (خارج هذا الكوميت)

بلوك الـ seed لا يصحح مفتاحاً **موجوداً بمحتوى خاطئ**. التحقق والتصحيح:

1. تشخيص القيمة الحية:

```sql
SELECT value->'catVeh' AS cat_veh, value->'carDocTp' AS car_docs, value->'plateTp' AS plate_types
FROM public.app_config WHERE key='main';
```

2. إن كانت `catVeh` منسوخة من العقارات — استبدالها:

```sql
UPDATE public.app_config
SET value = value || $fix$
{
  "catVeh": {
    "0": {"nm": "سيارة سياحية", "sub": ["سيدان", "هاتشباك", "دفع رباعي (SUV)", "كوبيه", "كابروليه", "ستيشن"]},
    "1": {"nm": "بيك أب / فان", "sub": ["بيك أب", "فان", "ميني باص"]},
    "2": {"nm": "شاحنة / نقل",  "sub": ["شاحنة صغيرة", "شاحنة كبيرة", "صهريج", "براد"]},
    "3": {"nm": "دراجة نارية",  "sub": ["سكوتر", "رياضية", "طرق وعرة"]},
    "4": {"nm": "آليات ومعدات", "sub": ["جرار زراعي", "حفارة", "رافعة", "مولدة كهربائية"]},
    "5": {"nm": "باص",          "sub": ["باص كبير", "ميكروباص"]}
  }
}
$fix$::jsonb
WHERE key = 'main';
```

(ونفس الأسلوب لـ `carDocTp`/`plateTp` إن كانت مفاتيحها غير رقمية أو مفقودة.)

3. بعد تسجيل الخروج/حذف كاش التطبيق (Hive config cache) أو الانتظار — المزود يحدّث الكاش عند كل fetch ناجح.

## اختبارات يدوية مقترحة حسب الدور

- **مستخدم/وسيط**: إضافة عرض سيارة كامل (تصنيف سيارات + محافظة + سند سيارة + نمرة) وعرض عقار كامل (5 خطوات + معاينة) — التنقل بأزرار التالي/رجوع.
- **موظف/نائب/مدير**: `/admin/add-offer` عبر المدخل الجديد، اختيار صاحب عرض، نشر عرض نيابة عنه.
- **مقاومة الداتا السيئة**: وضع مفتاح غير رقمي مؤقت بـ carDocTp والتأكد أن الشاشة لا تتجمد ويعرض التحذير بدلها.
