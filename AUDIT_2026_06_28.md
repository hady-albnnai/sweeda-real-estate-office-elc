# تقرير تدقيق استقرار — عقارات السويداء
**التاريخ:** 2026-06-28 16:50 Asia/Damascus  
**الفرع:** main @ 61030f4 “Fix remaining profile analyzer info”  
**المرجع:** LOGIC_SPEC.md / DEVELOPMENT_GUIDELINES.md / CURRENT_STATUS.md

---
## 1. ملخص تنفيذي

التطبيق أمنياً ممتاز (100% Phase 8+9+10 مُنجزة، كل RPCs الحساسة مقفولة لـ service_role).
وظيفياً: **غير مستقر للإطلاق**.

- `flutter analyze` لا يمكن تشغيله محلياً في هذه البيئة، آخر ملف `analysis_out.txt` قديم (ae422ec). الكود الحالي نظرياً 0 errors بعد سلسلة commits: 91181e0, 69e2732, c520539, 61030f4.
- بقي **12 استدعاء RPC مباشر** في الـ client، **6 منها مقفولة أمنياً** وستفشل حتماً.
- نظام النقاط والإشعارات **معطل بالكامل** (مقصود مؤقتاً حسب CURRENT_STATUS).
- Edge Functions: 29 دالة منشورة، التغطية في الـ client جيدة: user-* مكتملة، admin-* مكتملة، لكن `broker-actions` = **0 استدعاء** — الوسيط لا يزال يستخدم RPC مباشر.
- لا يوجد `print` / `debugPrint` / TODO في الكود — ملتزم بالدستور.

**الخلاصة:** التطبيق يبني، لكن 4 تدفقات أساسية مكسورة: النقاط، الإشعارات، الدفع، إجراءات الوسيط.

---
## 2. خريطة Edge Functions ↔ Providers

| Edge Function | استدعاءات في lib | الحالة |
|---|---|---|
| user-account | 13 | ✅ يعمل |
| user-offers | 8 | ✅ يعمل |
| user-requests | 8 | ✅ يعمل |
| user-appointments | 8 | ✅ يعمل |
| user-notifications | 4 | ✅ يعمل |
| admin-offers | 2 | ✅ |
| admin-verifications | 1 | ✅ |
| admin-payments | 1 | ✅ |
| admin-appointments | 2 | ✅ |
| admin-dashboard | 6 | ✅ |
| admin-deals | 3 | ✅ |
| admin-reports | 2 | ✅ |
| executor-tasks | 10 | ✅ |
| photographer-tasks | 3 | ✅ |
| broker-actions | **0** | ❌ غير مستخدم |
| create-user / update-user-role / … | عبر StaffAdminService | ✅ |

**ملاحظة:** broker_provider لا يزال يستدعي RPC مباشر بدل `broker-actions`.

---
## 3. استدعاءات RPC المباشرة المتبقية (12)

| الملف | السطر | RPC | الحالة الأمنية حسب CURRENT_STATUS |
|---|---|---|---|
| `business_service.dart:29` | `addPoints` → `add_points` | **مقفول** service_role فقط (2026-06-17) | ❌ سيفشل |
| `business_service.dart:43` | `awardPointsSafe` → `award_points_safe` | **مقفول** service_role فقط | ❌ سيفشل |
| `business_service.dart:62` | `update_user_badge` | غير موثق بالقفل، غالباً مفتوح | ⚠️ |
| `business_service.dart:356` | `register_daily_streak_internal` | مفتوح؟ يحتاج تحقق | ⚠️ |
| `business_service.dart:418` | `mark_social_published_internal` | ؟ | ⚠️ |
| `business_service.dart:438` | `check_offer_duplicate` | مفتوح للقراءة | ✅ |
| `auth_provider.dart:326` | (dynamic) – راجع grant | ؟ | ⚠️ |
| `broker_provider.dart:67` | ؟ افتح الملف | ؟ | ❓ |
| `broker_provider.dart:84` | ؟ | ؟ | ❓ |
| `payment_provider.dart:30` | `create_payment_internal` | المدفوعات نُقلت لـ admin-payments، إنشاء الدفع من المستخدم يجب أن يكون عبر Edge | **محتمل مقفول** |
| `become_broker_screen.dart:68` | `submit_broker_request_internal` | ؟ | ⚠️ |
| `boost_offer_screen.dart:88` | `purchase_offer_boost` | حسب 2026-06-28: مقفول anon/authenticated، ممنوح service_role فقط — الترقية يجب أن تكون عبر `user-offers` | ❌ مزدوج: يوجد استدعاء RPC مباشر + Edge |
| `staff_admin_service.dart:177` | ؟ | غالباً `get_staff_stats_internal` | ⚠️ |
| `auth_service.dart:237` | `revoke_staff_session` | جلسات الموظفين — آمن | ✅ |
| `rating_dialog.dart:82` | ؟ | تقييم | ⚠️ |

**النتيجة:** 3-6 RPCs **مؤكد فشلها** لأنها مقفولة عمداً:
- `add_points`
- `award_points_safe`
- `purchase_offer_boost`
- (محتمل) `create_payment_internal`
- `notify_user` معلّق بالتعليقات (صح)

---
## 4. الأنظمة المتأثرة

### 4.1 النقاط (pts) — معطّل
- `BusinessService.addPoints` / `awardPointsSafe` → 403 Forbidden
- الأثر: تسجيل دخول يومي، إضافة عرض، دعوة صديق، تقييم 5 نجوم — لا نقاط.
- الرتب السلوكية (badge) تتجمد.
- **مطابق للدستور §3.2 لكن مخالف لوظيفة التطبيق.**

### 4.2 الإشعارات — معطّل جزئياً
- `notify_user` / `send_push_notification` مقفولة service_role.
- الإنشاء يجب أن يكون Triggers فقط. حالياً بعض الأحداث لا تُنشئ إشعار.
- NotificationProvider يقرأ عبر `user-notifications` Edge ✅ لكن الإنشاء مكسور.

### 4.3 ترقية العرض (Boost)
- يوجد مساران متضاربان:
  - `boost_offer_screen.dart` يستدعي `purchase_offer_boost` RPC مباشر ❌ مقفول
  - `user-offers` Edge يدعم boost ✅
- يجب حذف المسار RPC.

### 4.4 الدفع
- `PaymentProvider.makePayment` → `create_payment_internal` RPC مباشر.
- حسب الخطة: المدفوعات أُغلقت ونُقلت لـ `admin-payments`.
- **هل إنشاء دفع من المستخدم مسموح؟** غير واضح – يحتاج قرار.

### 4.5 الوسيط
- `broker_provider.dart` سطران RPC مباشر، و `broker-actions` Edge **= 0 استدعاء**.
- الوسيط غالباً مكسور أو يستخدم مسارات قديمة.

### 4.6 التقييم
- `rating_dialog.dart:82` RPC مباشر — تحقق هل `ratings` insert مسموح RLS أم عبر RPC؟
- حسب LOGIC_SPEC §5.3: التقييم محمي triggers — يحتاج اختبار.

---
## 5. فحص جودة الكود

- `print` / `debugPrint`: **0** فعلية (1 تعليق فقط) ✅
- TODO / FIXME: **0** ✅
- Imports غير مستخدمة: لا يمكن التحقق بدون `flutter analyze`، آخر commit يدّعي تنظيفها.
- BuildContext across async gaps: كانت كثيرة في analysis_out القديم، آخر 4 commits تدّعي إصلاحها.
- الملفات الحساسة:
  - `become_broker_screen.dart` ✅ مُصلّح (RadioListTile)
  - `user_details_screen.dart` ✅ مُصلّح (لا يوجد `admin` undefined)
- `pubspec.yaml`: `http` و `http_parser` مضافان صراحة ✅

---
## 6. اختبار وظيفي سريع (نظري حسب الكود)

| المسار | متوقع |
|---|---|
| زائر يتصفح /home | ✅ يعمل |
| تسجيل OTP SMS / WhatsApp | ✅ عبر `verify-sms-otp` Edge |
| setup_profile (username+pwd) | ✅ |
| نشر عرض | ✅ عبر `user-offers` |
| حجز موعد | ✅ عبر `user-appointments` |
| ترقية عرض بالنقاط | ❌ RPC مقفول + نقاط صفر |
| كسب نقاط يومية | ❌ مقفول |
| إشعارات فورية | ⚠️ جزئي |
| إنشاء طلب عميل | ✅ عبر `user-requests` |
| دورة حياة الطلب (إغلاق/تجديد) | ✅ سيرفر |
| لوحة إدارة / موظف | ✅ Edge |
| وسيط – لوحته | ⚠️ يستخدم RPC مباشر – خطر 403 |
| دفع باقة | ❓ `create_payment_internal` قد يفشل |
| تقييم بعد موعد | ⚠️ يحتاج اختبار |

---
## 7. توصية العمل — خطة استقرار 48 ساعة

### Phase A — تثبيت أخضر (2-3 ساعات)
1. تشغيل `flutter analyze` محلي عندك، أو نثبت Flutter هنا — للتأكد 0 errors فعلاً.
2. حذف/تعطيل استدعاءات النقاط المكسورة مؤقتاً في `BusinessService` (return false مع تعليق واضح) حتى لا تكسر UX بـ exceptions.
3. توحيد Boost: احذف RPC المباشر في `boost_offer_screen.dart`، اعتمد `user-offers` فقط.
4. فحص `broker_provider.dart` — إما انقل لـ `broker-actions` Edge أو وثّق أنه مقفول.
5. commit: `stabilize: disable broken points RPCs, unify boost to Edge`

### Phase B — إعادة تفعيل النقاط بأمان (4-6 ساعات)
- خياران حسب الدستور:
  - **سريع:** Edge Function `user-rewards` صغيرة تستدعي `award_points_safe` بـ service_role بعد التحقق.
  - **أنظف:** Triggers في DB لكل حدث (add_offer → points, streak → points …)
- أنصح Edge سريع الآن، Trigger لاحقاً.
- بعدها تعود الرتب السلوكية للعمل.

### Phase C — Smoke Test دوري كامل
استخدم `docs/MANUAL_TEST_PLAN_BY_ROLE.md` بالترتيب:
1. زائر (15 د)
2. مستخدم جديد OTP → نشر عرض → طلب (30 د)
3. وسيط (20 د)
4. موظف مكتب / منفذ / مصور (30 د)
5. إدارة كاملة (45 د)
- سجّل أي 403 / 401 — غالباً RPC منسي.

### Phase D — تجميد
- لا قفل RPC جديد حتى نُطلق 1.0.0
- وثّق كل Edge Function في `FUNCTIONS_REFERENCE.md`
- ارفع versionCode + ابنِ APK
- `git tag v1.0.0-rc1`

---
## 8. قرارات مطلوبة منك الآن

1. **النقاط:** نعطلها مؤقتاً بصمت حتى لا تكسر التطبيق؟ أم نبني `user-rewards` Edge فوراً؟
2. **الدفع:** هل `create_payment_internal` يجب أن يبقى RPC مفتوح للمستخدم لإنشاء دفعة، أم ننقله لـ Edge `user-payments`؟
3. **الوسيط:** ننقل `broker_provider` لـ `broker-actions` Edge الآن أم نؤجل؟
4. **هل تريد أثبت Flutter هنا وأشغل `flutter analyze` و `flutter test` فعلياً الآن؟** أستطيع تثبيته (~5 دقائق) ونحصل على تقرير دقيق 100% بدل التخمين التحليلي.

---
**الخلاصة النهائية:** أمنياً أنت في القمة. وظيفياً التطبيق **مكسور في 4 عقد: points, notifications, boost, broker**. نقدر نرجعه “جاهز للإطلاق” خلال يوم عمل واحد إذا جمّدنا القفل الأمني وركزنا على إعادة التوصيل Edge→Provider.

— تقرير مُعد آلياً مع مراجعة يدوية، ملتزم بـ DEVELOPMENT_GUIDELINES / LOGIC_SPEC §5+§6.
